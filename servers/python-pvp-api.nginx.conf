# API: internet → HTTPS → localhost:3000
server {
    listen 443 ssl;
    server_name api.coding-master.kelvin-test.xyz;

    ssl_certificate     /etc/letsencrypt/live/api.coding-master.kelvin-test.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.coding-master.kelvin-test.xyz/privkey.pem;

    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        client_max_body_size 5m;
    }
}

# Web frontend: internet → HTTPS → localhost:3001
server {
    listen 443 ssl;
    server_name coding-master.kelvin-test.xyz;

    ssl_certificate     /etc/letsencrypt/live/api.coding-master.kelvin-test.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.coding-master.kelvin-test.xyz/privkey.pem;

    location / {
        proxy_pass         http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}

# Internal: VPC Lambda → HTTP (port 80) → localhost:3000
server {
    listen 80;
    server_name _;

    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        client_max_body_size 5m;
    }
}
